# Local development profile. Never use this profile for public production deployment.
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
fixmate.jwt.secret=${JWT_SECRET:dev-only-fixmate-jwt-secret-change-me-please}
fixmate.admin.password=${ADMIN_PASSWORD:DevAdmin@123}
fixmate.admin.seed-enabled=${ADMIN_SEED_ENABLED:true}
fixmate.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:3000}
